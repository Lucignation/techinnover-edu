import styles from '../../styles/profile.module.css'

const Profile = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.heading}>Congrats, you have successfully logged in</h1>    
    </div>
  );
};

export default Profile;
